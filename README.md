# Projet-Backend

Necessities :

Express
MySQL
Nodemon


Deploy :

After copied the repertory, use in the terminal the command in order :
"npm install" for install the dependencies
"npm start" for run

Setting :

Change the password for your database in db/db.js
